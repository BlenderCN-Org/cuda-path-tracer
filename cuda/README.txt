### Prerequisites:
Eigen3 needs to be installed.
Cuda toolkit 8 needs to be installed.

### Compiling
Norigl can be compiled by:

mkdir build
cmake ..
make -j8


### Run
To render the final scene:

./norigl ../../scenes/proj/final.xml
